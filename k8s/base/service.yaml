apiVersion: v1
kind: Service
metadata:
  name: pocket-aide
spec:
  type: ClusterIP
  selector:
    app: pocket-aide
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
